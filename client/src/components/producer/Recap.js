import React from 'react'

const Recap = (props) => {

    const { assetName, modifiedAssetName, assertions, protectedAssertionList, setOutputName } = props;


    return (
        <div>Recap</div>
    )
}

export default Recap